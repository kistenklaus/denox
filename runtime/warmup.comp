#version 450

layout(local_size_x = 256) in;

layout(set = 0, binding = 0) buffer Buf {
  float data[];
};

void main() {
  uint idx = gl_GlobalInvocationID.x;
  float x = data[idx];

  for (int i = 0; i < 5000; ++i) {
    x = x * 1.000001 + float(i & 1);
  }

  data[idx] = x;
}
